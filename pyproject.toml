[project]
name = "mongopython"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fieldmarcel2", email = "tripathi20t@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
]

[project.scripts]
mongopython = "mongopython:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
